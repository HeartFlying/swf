为你整合了一套**基于Agent+Skill的全流程用户需求分析落地方案**，覆盖前期架构设计、Skill标准化、阶段化Plan执行、格式校验、用户交互、轻量化模式切换等所有讨论内容，形成**自动化、标准化、可落地、高适配**的闭环体系，既支持用户信息缺失时的全流程挖掘，也适配信息完整时的极速执行，直接可落地到实际Agent平台配置。


# 基于Agent+Skill的用户需求分析全流程落地方案
## 方案核心目标
通过**Agent分工协作+Skill原子化执行**，实现用户需求分析的全流程自动化，从原始模糊需求到可直接对接产品/开发的结构化落地结论，兼顾**流程标准化、结果可追溯、场景高适配**，同时规避LLM上下文窗口限制，支持用户精准参与流程，且能根据用户信息完整度自动切换执行模式。

## 一、整体架构设计：多Agent分工协作体系
采用**「1主协调+3专项执行」**的轻量多Agent架构，各Agent职责单一、低耦合，通过中心化信息库实现数据互通，无跨Agent全量数据传递，规避LLM上下文过载问题。

### 1. 主协调Agent（核心大脑）
- **核心职责**：接收用户原始需求→执行Plan制定→阶段化任务调度→模式自动切换→结果最终整合→信息库统一管理；
- **关键能力**：内置信息完整度评分规则，可自动判定并切换「常规执行模式」/「轻量化快速执行模式」。

### 2. 格式校验守卫Agent（流程守卫）
- **核心职责**：作为所有Skill执行的前置/后置校验节点，保障输入输出格式标准化、上下游数据关联有效性；
- **关键能力**：仅做格式/字段/关联校验，不干预业务逻辑，校验失败触发Skill重跑，3次重跑失败则暂停阶段并反馈。

### 3. 用户交互Agent（唯一交互入口）
- **核心职责**：承接主协调Agent的交互指令→向用户发起定向提问→标准化解析用户输入→反馈给主协调Agent；
- **关键能力**：被动触发交互，仅定向提问不新增问题，用户输入格式不规范时仅提醒一次，输出为JSON格式方便解析。

### 4. 技能执行Agent（业务执行）
- **核心职责**：按主协调Agent的调度，串行执行对应阶段的Skill，输出标准化产物，接收校验Agent的重跑指令；
- **拆分原则**：可按业务模块拆分为「采集执行Agent」「分析执行Agent」，轻量版可合并为单技能执行Agent。

### 5. 中心化需求分析信息库（数据底座）
- **核心定位**：所有Plan、Skill、阶段的产物均按唯一ID持久化存储，是各Agent数据传递的唯一载体；
- **存储规则**：采用「固定9字段+元信息补充」的标准化格式，支持飞书/Excel表格、JSON文件、本地Markdown三种落地形式；
- **核心能力**：支持按需调取、断点续跑、全流程追溯，阶段间仅传递产物ID和核心摘要，压缩Token。

## 二、核心原子化Skill体系：13个Skill全量定义
共设计**13个原子化Skill**（1个Plan制定+12个业务分析），所有Skill均遵循**「固定头部元信息+标准化主体结构」**的统一输出格式，主体内容为固定字段的Markdown表格，无自由文本，上下游通过**需求序号强关联**实现无缝衔接。

### 1. S001：Plan制定（主协调Agent专属）
- **输入**：用户原始需求；
- **输出**：《需求分析阶段化执行Plan》（分≤4个阶段，低耦合、无交叉依赖，含阶段目标/执行Skill/输出物/依赖关系）；
- **核心约束**：仅输出标准化表格+4条执行规则，无冗余描述，严格控制Token量。

### 2. S1阶段（需求边界与原始采集）- S101~S104
**核心目标**：明确需求边界，提取显/隐性需求，完成用户验证，输出《最终需求原始表》；
- **S101：需求边界界定**：按产品目标/目标受众/核心场景/资源约束拆解边界，输出固定字段表格；
- **S102：显性需求提取**：从用户需求中提取显性需求，按需求序号/内容/来源输出表格；
- **S103：隐性需求挖掘**：通过5W2H挖掘隐性需求，按显性需求关联序号/内容/挖掘依据输出表格；
- **S104：需求验证**：将显/隐性需求反馈用户验证，按需求类型/内容/验证结果/修正后内容输出表格（S1阶段汇总产物）。

### 3. S2阶段（市场与需求价值校验）- S201~S202
**核心目标**：验证需求市场价值，分析竞品差异，输出《需求市场价值校验清单》；
- **S201：竞品分析**：调研头部竞品，按竞品名称/需求覆盖度/优势/短板/启示输出表格；
- **S202：市场痛点验证**：关联S1需求序号，按需求内容/痛点真实性/市场意愿/处理结论输出表格（S2阶段汇总产物）。

### 4. S3阶段（技术可行性与选型）- S301~S303
**核心目标**：识别需求风险，判断技术可行性，完成轻量化技术选型，输出《技术选型及成本表》；
- **S301：需求风险识别**：关联S2需求序号，按风险类型/描述/等级/规避建议输出表格；
- **S302：技术可行性评估**：关联S2需求序号，按实现难度/技术瓶颈/可行性结论/备注输出表格；
- **S303：技术选型**：针对可行需求，按需求模块/技术栈/核心组件/实施周期/成本/选型理由输出表格（S3阶段汇总产物）。

### 5. S4阶段（需求整合与核心提炼）- S401~S403
**核心目标**：完成需求分类与优先级排序，提炼核心需求，输出《需求分析最终报告》；
- **S401：需求分类梳理**：关联S3需求序号，按需求内容/分类/子分类输出表格；
- **S402：需求优先级排序**：关联S401序号，按需求内容/优先级/排序依据输出表格；
- **S403：核心需求提炼**：提炼1-3个核心需求，按核心需求序号/内容/支撑原始需求/核心价值输出表格（全流程最终核心产物）。

### 6. 所有Skill统一输出模板
```markdown
# 【SkillX+名称】输出产物
## 元信息
- 所属PlanID：{XXX} （Agent自动填充）
- 所属阶段：{SX} （Agent自动填充）
- 前置依赖Skill：{SkillX/SkillX+SkillX} （无则填“无”）
- 产物唯一ID：{XXX-SX-SX-00X} （Agent自动填充）
- 核心内容摘要：{≤50字，提炼核心结论}
- 用户交互记录：{无/用户补充/用户确认} （有则填具体内容，无则填“无”）
## 主体内容
{Skill专属固定字段Markdown表格，无增删、无自由文本}
```

## 三、阶段化执行流程：Plan制定→分阶段执行→结果整合
核心遵循**「先定Plan，再分阶段执行」**原则，将全流程拆分为4个低耦合阶段，阶段间仅通过「上一阶段汇总产物ID+核心摘要」传递信息，单阶段内Skill串行执行，全程规避LLM上下文窗口限制，形成**「Plan制定→阶段初始化→单阶段执行→阶段联动→最终整合」**的自动化闭环。

### 1. 全流程执行步骤（主协调Agent主导）
1. **Plan制定**：接收用户原始需求→执行S001生成阶段化Plan→为Plan生成唯一6位ID→同步至信息库；
2. **模式判定**：对用户原始需求做**信息完整度评分**（4维度，满分100），≥90分启动轻量化模式，否则启动常规模式；
3. **阶段初始化**：读取Plan，从S1开始，加载当前阶段Skill清单+空白模板+上一阶段汇总产物（S1无前置）→调度技能执行Agent；
4. **单阶段闭环执行**：
   - 前置校验：调用校验Agent，检查输入数据格式/字段/关联有效性，校验失败则重跑（最多3次）；
   - Skill执行：技能执行Agent按顺序执行当前阶段Skill，输出标准化产物；
   - 后置校验：调用校验Agent，检查输出产物的元信息/主体表格/字段/摘要规范，校验失败则重跑（最多3次）；
   - 产物存储：校验通过后，将产物按唯一ID存入信息库，更新执行状态为「已完成」；
5. **阶段联动**：当前阶段所有Skill执行完成后，调取该阶段汇总产物ID+核心摘要→作为下一阶段输入→重复步骤4，直至S4完成；
6. **最终整合**：主协调Agent从信息库按需调取各阶段核心产物→按模板整合为《用户需求分析最终报告》+全量配套表格→输出给用户/下游环节。

### 2. 4个阶段核心关联规则
- **S1→S2**：S2仅调取S1的S104《最终需求原始表》，通过需求序号强关联；
- **S2→S3**：S3仅调取S2的S202《需求市场价值校验清单》，通过需求序号强关联；
- **S3→S4**：S4仅调取S3的S303《技术选型及成本表》，通过需求序号强关联；
- **跨阶段约束**：仅允许前一阶段汇总产物对接后一阶段首个Skill，禁止跨多阶段直接关联原始需求。

### 3. 同阶段内Skill联动规则
1. 同阶段内Skill串行执行，下游Skill必须通过**需求序号**与上游Skill输出表格强关联；
2. 下游Skill直接复用上游Skill的「需求内容」字段，不做二次改写，确保数据一致性；
3. 阶段内最后一个Skill为**阶段汇总产物**，是该阶段对接下一阶段的唯一数据载体。

## 四、格式校验体系：前置+后置全流程校验
由**格式校验守卫Agent**实现全流程自动化校验，分为**通用校验规则**和**分Skill专项校验规则**，仅针对格式/字段/关联做校验，不干预业务逻辑，校验失败触发重跑机制，保障所有Skill输入输出的标准化和上下游的无缝衔接。

### 1. 通用前置校验规则（所有Skill通用）
- 输入数据必须为Markdown表格，无自由文本、无多余注释；
- 输入必须包含当前Skill所需核心字段，无缺失；
- 若存在上游关联，必须包含有效关联字段（如需求序号），无无效/缺失关联。

### 2. 通用后置校验规则（所有Skill通用）
- 输出必须包含完整元信息，无缺失字段，核心摘要≤50字；
- 主体内容为固定字段Markdown表格，无增删、无修改字段名、无自由文本；
- 产物唯一ID符合「PlanID-阶段编号-Skill编号-序号」格式；
- 元信息中的「用户交互记录」如实填写，无遗漏。

### 3. 分Skill专项校验规则（核心约束）
- S1阶段：S101边界类型为固定值，S102需求序号连续，S103关联序号与S102一致，S104验证结果为固定枚举；
- S2阶段：S201覆盖度为固定枚举，S202处理结论/市场意愿为固定枚举；
- S3阶段：S301风险类型/等级为固定枚举，S302可行性结论为固定枚举，S303实施周期/成本格式规范；
- S4阶段：S401需求分类为固定枚举，S402优先级为MoSCoW模型固定值，S403核心需求序号连续。

### 4. 异常处理规则
- 单个Skill前置/后置校验失败，**最多触发3次重跑**，3次均失败则暂停当前阶段；
- 阶段暂停后，主协调Agent反馈**详细错误详情**，人工介入修复后可**断点续跑**，无需从头执行；
- 校验通过后，校验Agent自动在信息库中更新产物「执行状态」为「已完成」。

## 五、用户参与体系：被动触发+定向交互+标准化输入
用户仅通过**用户交互Agent**参与流程，采用**「被动触发、定向提问、标准化接收、断点续跑」**原则，不打乱自动化执行节奏，仅在信息缺失/模糊、核心产物需确认、高风险需决策时触发交互，全程做到“该问才问、问则精准”。

### 1. 3个核心交互触发节点
#### 节点1：S1阶段信息缺失/模糊时（高频）
- 触发场景：S101/S102执行后，校验Agent判定核心字段（如受众/场景/需求内容）为空/模糊；
- 交互形式：定向补全提问，如【需求分析-S1-S101】请补充目标受众细分，按「XX年级-XX年级」格式填写。

#### 节点2：核心产物需用户确认时（中高频）
- 触发场景：S104（需求验证）、S202（市场校验）、S403（核心提炼）执行完成后，需用户人工确认结果；
- 交互形式：发送标准化表格+确认提问，如【需求分析-S1-S104】请确认以下需求清单，是否需要修改？确认/需修改，修改请按「需求序号+修正内容」格式填写。

#### 节点3：S3阶段高风险需决策时（低频）
- 触发场景：S301（风险识别）标记高风险、S302（技术可行性）判定需论证/不可行；
- 交互形式：定向决策提问，如【需求分析-S3-S302】需求「AI智能批改」技术实现高风险，算力成本预估5万/月，请问是否继续保留？保留/剔除/降级为简易版。

### 2. 3类用户标准化输入格式
用户交互Agent在提问时明确格式要求，输入结果可直接解析，无二次处理，避免格式混乱：
1. **选择题**：用于确认/决策类问题，如用户输入「保留」「小学」；
2. **补充类**：用于字段补全/细分类问题，按指定格式填写，如「小学3-6年级」「2周」；
3. **修改类**：用于清单/表格修改，按「需求序号+修正内容」格式，多个用分号分隔，如「1+支持英语刷题；2+取消成绩排行榜」。

### 3. 用户交互联动流程
1. 主协调Agent判定需要用户交互→向用户交互Agent发送**标准化交互指令**（阶段+Skill+问题+格式要求）；
2. 用户交互Agent按格式向用户发起提问，接收用户输入；
3. 若用户输入格式不规范，仅提醒一次，用户重新输入后解析；
4. 用户交互Agent将输入解析为**JSON格式**（阶段+Skill+问题+答案）→反馈给主协调Agent；
5. 主协调Agent将用户补充/确认信息作为Skill新增输入→重新调度Skill执行→流程继续自动化执行；
6. 技能执行Agent将用户交互记录填入产物元信息→同步至信息库，实现全流程追溯。

## 六、双模式执行体系：常规模式+轻量化快速执行模式
主协调Agent通过**信息完整度评分**自动切换执行模式，**保留全量Skill架构**，仅通过「跳过冗余步骤、合并执行、极简校验、默认填充非核心内容」实现轻量化执行，在保障标准化和可追溯的前提下，大幅提升信息完整场景的执行效率。

### 1. 信息完整度评分规则（满分100，≥90分触发轻量化模式）
按4个核心维度对用户原始需求打分，主协调Agent自动计算，无人工干预：
1. **边界清晰度（25分）**：明确产品目标、目标受众、核心使用场景，每项8分，完整得25分；
2. **需求具体度（25分）**：明确显性功能需求、非功能需求，需求项可量化，每项12分，完整得25分；
3. **约束明确度（25分）**：明确时间、预算、技术、资源约束，每项6分，完整得25分；
4. **目标明确度（25分）**：明确产品核心价值、商业目标、落地指标，每项12分，完整得25分。

### 2. 常规执行模式（评分＜90分，信息缺失/模糊）
- **Skill执行**：全Skill分步深度执行，S103深度挖掘隐性需求，S104发起用户验证；
- **校验规则**：全字段严格校验（核心+非核心），前置/后置校验均执行；
- **用户交互**：按3个触发节点高频触发，需用户补充/确认/决策；
- **执行节点**：每Skill1个执行节点，分步调度、分步存储、分步校验；
- **输出物**：全量标准化产物，包含所有细节，支持全链路详细追溯。

### 3. 轻量化快速执行模式（评分≥90分，信息完整清晰）
- **Skill执行**：全Skill保留，合并极简执行；S103自动跳过或快速匹配少量隐性需求；S104免用户确认，直接标记为「无需验证」；
- **校验规则**：仅校验核心字段（需求序号/内容/核心结论），免校验非核心字段（挖掘依据/需求来源/选型理由细节）；
- **用户交互**：全程无用户交互，免所有补充/确认/决策步骤；
- **执行节点**：按阶段合并节点，每阶段1个执行节点，一次性执行全阶段Skill、一次存储、一次校验；
- **非核心内容**：自动填充标准化默认值，如S201竞品分析短板填「用户需求已形成差异化，无明显短板」，S301风险识别填「无明显风险，等级低」；
- **输出物**：核心标准化产物，保留核心结论和表格，简化非核心细节，支持阶段级核心链路追溯。

### 4. 双模式核心对比
| 对比维度 | 常规执行模式 | 轻量化快速执行模式 |
| :--- | :--- | :--- |
| Skill执行 | 分步深度执行 | 阶段合并极简执行 |
| 用户交互 | 高频触发，多轮交互 | 全程无交互，免确认 |
| 校验规则 | 全字段严格校验 | 仅核心字段校验，免非核心 |
| 执行节点 | 每Skill1节点，分步调度 | 每阶段1节点，合并调度 |
| 非核心内容 | 深度分析填写 | 标准化默认值自动填充 |
| 执行效率 | 常规（5-10分钟/需求） | 极速（1-2分钟/需求） |
| 追溯粒度 | 全链路详细追溯 | 阶段级核心链路追溯 |
| 输出物 | 全量细节版标准化产物 | 核心结论版标准化产物 |

## 七、输出物体系：标准化可落地的全量交付物
所有执行结果最终整合为**1份核心交付物+6份配套标准化表格**，格式统一、字段固定，可直接对接产品设计、开发、项目管理环节，支持按需调取、修改、追溯。

### 1. 核心交付物
《用户需求分析最终报告》：整合各阶段核心结论，包含**需求边界、原始需求清单、市场竞品分析、技术可行性与选型、需求分类与优先级、核心需求提炼、风险与规避建议**七大模块，是需求分析的最终落地结论。

### 2. 6份配套标准化表格
1. 《需求边界清单》（S101输出）；
2. 《最终需求原始表》（S104输出，S1汇总）；
3. 《竞品需求差异分析表》（S201输出）；
4. 《需求市场价值校验清单》（S202输出，S2汇总）；
5. 《需求技术选型及成本表》（S303输出，S3汇总）；
6. 《核心需求提炼表》（S403输出，全流程最终核心）。

## 八、快速落地步骤（零重构，可直接配置）
1. **Agent配置**：在Agent平台新建「主协调Agent」「格式校验守卫Agent」「用户交互Agent」「技能执行Agent」，按方案复制各Agent的Prompt配置；
2. **Skill配置**：为13个Skill编写标准化输入输出规则，按方案固定主体表格字段，配置统一输出模板；
3. **信息库搭建**：选择飞书/Excel表格、JSON文件、本地Markdown其中一种形式，按方案的9个核心字段搭建中心化信息库；
4. **规则嵌入**：在主协调Agent中嵌入**信息完整度评分规则**「双模式切换规则」「阶段化调度规则」，在校验Agent中嵌入**前置/后置校验规则**「异常处理规则」；
5. **流程测试**：
   - 常规模式测试：用模糊需求（如“做一个刷题APP”）跑通全流程，故意留信息缺失，测试用户交互和重跑机制；
   - 轻量化模式测试：用完整需求（如“做一个面向小学3-6年级的课后刷题APP，支持题库/刷题/成绩统计，无开发预算，需轻量化实现”）跑通全流程，验证模式自动切换和极速执行效果；
6. **细节优化**：根据测试结果，微调Skill字段、校验规则、默认值内容，适配实际业务场景。


### 方案核心优势
1. **全流程自动化**：从需求接收、Plan制定、阶段执行到结果整合，全程由Agent主导，仅在必要时触发用户交互，大幅减少人工介入；
2. **标准化可落地**：所有Skill、Agent、输出物均有固定格式和规则，结果可直接对接产品/开发，避免信息偏差；
3. **场景高适配**：支持双模式自动切换，既适配用户信息缺失的常规场景，也适配信息完整的极速场景，保留全量架构无需重构；
4. **可追溯易复盘**：中心化信息库实现全流程产物持久化存储，每个产物有唯一ID，支持断点续跑和全链路追溯；
5. **规避LLM限制**：阶段化执行+按需调取+Token压缩，彻底规避LLM上下文窗口过载问题，适配小窗口开源LLM和大窗口商用LLM。