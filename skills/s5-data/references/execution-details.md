# S5-A03 执行详细步骤

## 步骤 1：前置校验

**目标**：验证前置产物是否完整、格式是否正确

**操作**：
1. 检查架构视图设计文档是否存在（S5-A02产物）
2. 检查数据需求清单是否存在（S1-S102产物中的数据部分）
3. 检查非功能需求报告是否存在（S1-S104产物）
4. 检查架构愿景文档是否存在（S5-A01产物）
5. 验证产物 ID 是否匹配且连续
6. 验证 Markdown 报告格式是否规范

**验证规则**：
- 所有前置产物必须存在
- 产物状态必须为 completed
- 领域模型（逻辑视图）必须包含实体定义

**错误处理**：
- 产物缺失 → 返回错误 E001，要求先完成前置 Skill
- 格式错误 → 返回错误 E002，要求重新生成前置产物
- 领域模型无效 → 返回错误 E003，要求完善架构视图设计

---

## 步骤 2：读取前置产物

**目标**：加载所有前置产物内容

**操作**：
1. 读取架构视图设计文档，提取逻辑视图中的领域模型
2. 读取数据需求清单，提取数据实体、数据关系、数据量
3. 读取非功能需求报告，提取性能、可用性、一致性要求
4. 读取架构愿景文档，提取数据特征、安全等级
5. 建立数据实体追溯索引

**验证规则**：
- 文件必须可读
- 内容必须完整
- 编码必须为 UTF-8
- 领域模型必须包含实体和关系

**错误处理**：
- 文件读取失败 → 返回错误 E101，重试 3 次
- 内容解析失败 → 返回错误 E102，尝试修复或中止

---

## 步骤 3：数据特征分析

**目标**：基于输入数据自动分析数据特征

**操作**：
1. 分析数据结构类型（结构化/半结构化/非结构化）
2. 分析数据关系复杂度（简单关系/复杂关系/图关系）
3. 分析数据规模等级（小/中/大/海量）
4. 分析访问模式（读多写少/读写均衡/写多读少）
5. 分析一致性要求（强一致性/最终一致性）
6. 分析查询特征（点查/范围查/全文检索/聚合分析）

**分析维度**：

| 分析维度 | 分析内容 | 输出 |
|----------|----------|------|
| **数据结构** | 结构化/半结构化/非结构化 | 数据类型判定 |
| **数据关系** | 简单关系/复杂关系/图关系 | 关系复杂度判定 |
| **数据规模** | 数据量、增长率、保留周期 | 规模等级判定 |
| **访问模式** | 读多写少/读写均衡/写多读少 | 访问模式判定 |
| **一致性要求** | 强一致性/最终一致性 | 一致性级别判定 |
| **查询特征** | 点查/范围查/全文检索/聚合分析 | 查询类型判定 |

**验证规则**：
- 每个维度必须有明确判定结果
- 数据规模必须有数量级估计

---

## 步骤 4：存储方案推导

**目标**：基于数据特征自动推荐存储方案

**操作**：
1. 根据数据特征匹配存储类型
2. 评估多存储组合需求
3. 生成存储选型建议

**存储类型选择矩阵**：

| 数据特征 | 推荐存储类型 | 理由 |
|----------|--------------|------|
| 结构化数据 + 复杂关系 + 事务要求 | 关系型数据库 | ACID支持，成熟稳定 |
| 半结构化数据 + 灵活Schema | 文档数据库 | 灵活模型，水平扩展 |
| 键值对 + 高并发访问 | 键值存储 | 极高性能，简单模型 |
| 宽列 + 海量数据 | 列式数据库 | 高效存储，批量处理 |
| 图关系 + 复杂查询 | 图数据库 | 关系遍历优化 |
| 时序数据 + 高写入 | 时序数据库 | 时间序列优化 |
| 全文检索 + 模糊查询 | 搜索引擎 | 倒排索引，相关性排序 |
| 缓存 + 会话数据 | 内存数据库 | 亚毫秒级响应 |

**多存储组合策略**：

| 场景 | 推荐组合 | 分工说明 |
|------|----------|----------|
| 事务型应用 | 关系库 + 缓存 | 主存储+加速读 |
| 内容管理 | 文档库 + 搜索引擎 | 内容存储+全文检索 |
| 实时分析 | 时序库 + OLAP | 实时写入+批量分析 |
| 社交网络 | 图库 + 关系库 | 关系查询+事务数据 |
| 电商系统 | 关系库+缓存+搜索引擎 | 事务+加速+检索 |

**验证规则**：
- 存储选型必须基于数据特征分析
- 必须有选型理由

---

## 步骤 5：数据模型设计

**目标**：设计逻辑数据模型和物理数据模型

**操作**：

### 5.1 关系型数据模型设计

**实体到表的映射规则**：

| 领域概念 | 映射规则 | 示例 |
|----------|----------|------|
| 聚合根 | 主表 + 唯一主键 | 订单表（订单ID） |
| 实体 | 独立表/嵌入表 | 订单项表（独立） |
| 值对象 | 嵌入表/JSON字段 | 地址（嵌入用户表） |
| 多对多关系 | 关联表 | 用户角色关联表 |
| 继承关系 | 单表/类表/具体表继承 | 根据查询模式选择 |

**表设计原则**：

| 设计目标 | 设计原则 | 具体措施 |
|----------|----------|----------|
| 查询性能 | 反规范化 | 适当冗余，减少关联 |
| 写入性能 | 分区分片 | 按时间/范围分区 |
| 数据一致性 | 规范化 | 第三范式，消除冗余 |
| 扩展性 | 垂直/水平分割 | 冷热分离，读写分离 |

### 5.2 文档数据模型设计（如适用）

**集合设计策略**：

| 场景 | 设计策略 | 示例 |
|------|----------|------|
| 一对一关系 | 嵌入文档 | 用户+用户详情 |
| 一对少关系 | 嵌入数组 | 订单+少量订单项 |
| 一对多关系 | 引用+冗余 | 用户+订单（引用） |
| 多对多关系 | 双向引用 | 学生+课程 |

### 5.3 缓存数据模型设计

**缓存策略选择**：

| 策略 | 适用场景 | 实现方式 |
|------|----------|----------|
| Cache-Aside | 读多写少 | 应用管理缓存 |
| Read-Through | 简化应用逻辑 | 缓存层加载数据 |
| Write-Through | 强一致性 | 同步写缓存和存储 |
| Write-Behind | 高写入性能 | 异步写回存储 |

**缓存键设计**：

| 数据类型 | 键格式 | 示例 |
|----------|--------|------|
| 单条记录 | `实体:ID` | `user:12345` |
| 列表数据 | `实体:list:条件` | `orders:list:user:123` |
| 聚合数据 | `agg:维度:指标` | `agg:daily:sales` |

**验证规则**：
- 逻辑模型必须包含所有核心实体
- 物理模型必须定义主键
- 表关系必须明确

---

## 步骤 6：核心表结构设计

**目标**：详细设计核心表结构

**操作**：
1. 识别核心表（高频访问、核心业务、大数据量、复杂关系）
2. 设计每个核心表的字段清单
3. 设计主键、外键、索引
4. 设计约束条件

**核心表识别规则**：

| 规则 | 说明 | 示例 |
|------|------|------|
| 高频访问 | 日均访问>1万次的表 | users, orders |
| 核心业务 | 支撑核心业务流程的表 | orders, order_items |
| 大数据量 | 预估数据量>1000万的表 | logs, audit_trail |
| 复杂关系 | 关联>5个其他表的表 | products, users |

**表结构设计内容**：

| 内容 | 说明 |
|------|------|
| 表名 | 英文表名及中文名 |
| 用途说明 | 表的业务用途 |
| 预估数据量 | 数据量预估 |
| 字段清单 | 字段名、类型、约束、说明 |
| 索引 | 主键索引、唯一索引、普通索引 |
| 约束 | CHECK约束、外键约束 |
| 表关系 | 与其他表的关联关系 |

**验证规则**：
- 每个表必须有主键
- 核心表必须有索引设计
- 字段必须有类型和约束

---

## 步骤 7：数据流转设计

**目标**：设计数据生命周期和同步策略

**操作**：

### 7.1 数据生命周期管理

```
数据流转设计：
├── 数据产生
│   ├── 用户操作产生
│   ├── 系统生成
│   └── 外部导入
├── 数据处理
│   ├── 实时处理
│   ├── 批量处理
│   └── 流处理
├── 数据存储
│   ├── 热数据（高频访问）
│   ├── 温数据（中频访问）
│   └── 冷数据（归档）
└── 数据销毁
    ├── 过期删除
    ├── 合规删除
    └── 归档清理
```

### 7.2 数据同步策略

| 同步场景 | 推荐方案 | 一致性保证 |
|----------|----------|------------|
| 主从复制 | 数据库原生复制 | 最终一致性 |
| 跨库同步 | CDC + 消息队列 | 最终一致性 |
| 缓存同步 | 发布订阅/失效通知 | 最终一致性 |
| 搜索索引同步 | 双写/CDC | 最终一致性 |

**验证规则**：
- 数据生命周期必须完整
- 同步策略必须明确

---

## 步骤 8：用户交互（决策确认）

**目标**：在需要用户决策的场景主动与用户交互

**触发场景**：

| 场景 | 说明 | 示例 |
|------|------|------|
| 存储选型争议 | 多个数据库都满足需求 | PostgreSQL vs MySQL |
| 分库分表策略 | 数据量达到分片阈值 | 分片键和策略选择 |
| 一致性级别 | 强一致性与性能冲突 | 业务容忍度选择 |
| 数据保留策略 | 数据保留周期和归档 | 合规要求选择 |

**交互方式 1：提供选项**

```
基于数据特征分析，推荐以下存储方案：

A. 单一关系型数据库（推荐）
   - PostgreSQL / MySQL
   - 适合当前数据规模和关系复杂度
   - 团队技术栈匹配度高

B. 关系型 + 缓存组合
   - PostgreSQL + Redis
   - 提升读性能，适合读多写少场景
   - 增加运维复杂度

C. 文档数据库
   - MongoDB
   - 灵活Schema，适合快速迭代
   - 事务支持相对较弱

请选择存储方案（A/B/C）：
```

**交互方式 2：询问澄清**

```
检测到以下数据实体数据量预估较大：
- 操作日志表：预计每月 1000万 条记录
- 访问记录表：预计每月 5000万 条记录

处理方案：
A. 按月分区，保留最近12个月（推荐）
B. 按用户ID哈希分片
C. 独立时序数据库存储

请确认处理方案（A/B/C）：
```

**交互方式 3：确认假设**

```
基于需求分析，做出以下数据架构假设：

假设 1：核心业务数据采用强一致性，分析数据采用最终一致性
假设 2：用户会话数据保留7天，操作日志保留90天
假设 3：热点数据缓存TTL设置为1小时

请确认以上假设是否成立（是/否）：
如有不成立的假设，请说明：_____
```

**使用模板**：
- 使用 [user-interaction/clarification-template.md](user-interaction/clarification-template.md) 进行信息澄清
- 使用 [user-interaction/option-selection-template.md](user-interaction/option-selection-template.md) 进行选项选择

---

## 步骤 9：生成数据架构文档

**目标**：生成完整的数据架构设计文档

**操作**：
1. 填充所有必需章节
2. 生成 ER 图（Mermaid erDiagram）
3. 生成存储部署架构图
4. 生成数据生命周期图
5. 保存为 `{PlanID}-S5-A03-001.md`

**文档结构**：
1. 执行摘要
2. 数据特征分析
3. 存储架构设计
4. 数据模型设计
5. 核心表结构设计
6. 数据流转设计
7. 数据治理
8. 容量规划
9. 设计决策
10. 风险与约束

**验证规则**：
- 报告必须包含所有必需章节
- 数据必须完整准确
- 格式必须清晰易读

---

## 步骤 10：后置校验

**目标**：验证产物完整性和正确性

**校验内容**：
- 数据架构文档已生成且格式正确
- 所有核心实体已建模
- 核心表结构设计完整
- ER 图语法正确
- Todo-List 可更新

**错误处理**：
- 产物文件不存在 → 返回错误 E201，重新生成
- Markdown 格式错误 → 返回错误 E202，修复格式
- 表结构缺失 → 返回错误 E203，补充后重新生成
- ER 图生成失败 → 返回错误 E204，修复语法

---

## 步骤 11-13：Todo-List 更新与用户评审

**目标**：更新Todo-List，标记 S5-A03 完成，准备用户评审

**Todo-List更新规则**：遵循 [execution-flow-standard.md](execution-flow-standard.md) 中的"Todo-List更新标准规则"章节

**S5-A03特定更新时机**：
1. **执行开始**：步骤1完成后，状态：待执行 → 执行中
2. **用户交互后**：步骤8完成后，更新交互记录
3. **执行完成**：步骤10完成后，状态：执行中 → 已完成
4. **用户评审后**：步骤13完成后，根据评审结果更新状态

**用户评审**：

**评审流程**：遵循 [execution-flow-standard.md](execution-flow-standard.md) 中的"用户评审标准流程"章节

**评审展示内容**：
- 数据架构设计核心内容（存储选型、核心表、数据流转）
- 设计决策摘要
- 容量规划预估
- 风险识别
