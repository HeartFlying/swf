# Agent+Skill用户需求分析系统 - 主工作流程文档

## 文档信息

| 项目 | 内容 |
|-----|------|
| 文档名称 | 主工作流程文档 |
| 文档版本 | 1.0 |
| 最后更新 | 2026-03-24 |
| 适用范围 | Agent+Skill用户需求分析系统 |

---

## 一、系统概述

### 1.1 系统架构

Agent+Skill用户需求分析系统采用**主从协作架构**，通过文件系统实现状态持久化和数据传递。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用户 (User)                                         │
│                         需求输入 / 交互确认                                      │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                      主协调Agent (Coordinator)                                │
│  职责：接收需求 → 制定Plan → 调度执行 → 状态管理 → 结果整合                      │
│  核心功能：信息完整度评分 / 双模式切换 / 断点续跑检测 / 阶段化调度                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
        ┌─────────────────────────────┼─────────────────────────────┐
        │                             │                             │
        ▼                             ▼                             ▼
┌───────────────┐          ┌─────────────────┐          ┌───────────────┐
│ 格式校验守卫Agent │          │ 用户交互Agent      │          │ 技能执行Agent   │
│ (Validator)   │          │ (UserInteraction)│          │  (Executor)   │
│ 职责：格式校验   │          │ 职责：用户交互      │          │ 职责：执行Skill │
│ 前置/后置校验   │          │ 定向提问/输入解析   │          │ 串行执行/产物存储│
└───────────────┘          └─────────────────┘          └───────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         13个Skill技能库                                       │
│  S0: Plan制定 → S1: 需求边界 → S2: 市场校验 → S3: 技术选型 → S4: 需求整合        │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        中心化信息库 (Database)                                 │
│              status.json / resume.json / index.json / 产物文件                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 核心设计原则

| 原则 | 说明 |
|-----|------|
| **文件驱动** | 所有状态和数据通过文件系统持久化，支持断点续跑 |
| **模板规范** | 所有产物遵循统一模板，确保格式一致性 |
| **串行执行** | 同阶段内Skill按依赖顺序串行执行，保证数据一致性 |
| **校验闭环** | 每个Skill执行前后都有校验，确保数据质量 |
| **用户介入** | 关键节点触发用户交互，保证结果符合预期 |

---

## 二、Agent协作流程

### 2.1 Agent协作关系图

```
                              ┌─────────────────┐
                              │      用户        │
                              └────────┬────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
                    ▼                  ▼                  ▼
           ┌─────────────────────────────────────────────────────┐
           │              主协调Agent (Coordinator)               │
           │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
           │  │  初始化阶段  │  │  阶段执行   │  │  结果整合   │  │
           │  └─────────────┘  └─────────────┘  └─────────────┘  │
           └─────────────────────────────────────────────────────┘
                              │
           ┌──────────────────┼──────────────────┐
           │                  │                  │
           ▼                  ▼                  ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │   Validator  │  │UserInteraction│  │   Executor   │
    │  前置校验    │  │   用户交互    │  │  Skill执行   │
    │  后置校验    │  │   输入解析    │  │  产物生成    │
    │  异常处理    │  │   结果反馈    │  │  状态更新    │
    └──────────────┘  └──────────────┘  └──────────────┘
```

### 2.2 Agent调用规范

#### 2.2.1 主协调Agent → Validator Agent

**前置校验调用**（Skill执行前）：
```
调用时机：Executor执行Skill之前
输入参数：
  - skillId: 即将执行的Skill编号
  - inputData: Skill的输入数据
  - mode: 当前执行模式(normal/lightweight)
  
处理逻辑：
  1. 执行通用前置校验（输入存在性、必填字段、数据类型）
  2. 执行Skill专项前置校验
  3. 检查前置依赖产物是否存在
  
输出结果：
  - valid: true/false
  - errors: 错误列表（如有）
  - warnings: 警告列表（如有）
  - fixSuggestions: 修复建议
```

**后置校验调用**（Skill执行后）：
```
调用时机：Executor执行Skill之后
输入参数：
  - skillId: 刚执行的Skill编号
  - outputData: Skill的输出产物
  - outputPath: 产物文件路径
  
处理逻辑：
  1. 执行通用后置校验（产物存在性、格式正确性）
  2. 执行Skill专项后置校验
  3. 执行产物文件校验
  
输出结果：
  - valid: true/false
  - errors: 错误列表（如有）
  - fixedOutput: 修正后的产物（如可自动修复）
  - retrySuggestion: 重跑建议
```

#### 2.2.2 主协调Agent → UserInteraction Agent

**交互触发条件**：
1. **节点1-S1阶段信息缺失**：Skill1/Skill2执行后关键字段为空
2. **节点2-产物确认**：Skill4/Skill10/Skill8执行后需用户确认
3. **节点3-S3阶段风险决策**：Skill7/Skill11标记高风险项

**调用参数**：
```json
{
  "interactionId": "INST-{timestamp}-{random}",
  "triggerPoint": "initialization|execution|decision",
  "stage": "S1|S2|S3|S4",
  "skillId": "S01-S12",
  "questionType": "supplement|clarification|selection",
  "question": "具体问题内容",
  "format": "输入格式要求",
  "context": { "相关上下文信息": "..." },
  "options": ["选项1", "选项2"],
  "timeout": 300
}
```

**输出结果**：
```json
{
  "interactionId": "INST-xxx",
  "userInput": "用户输入内容",
  "inputType": "choice|supplement|modify",
  "parsedData": { "解析后的结构化数据": "..." },
  "timestamp": "2026-03-24T10:00:00Z"
}
```

#### 2.2.3 主协调Agent → Executor Agent

**执行指令格式**：
```json
{
  "executionId": "EXEC-{timestamp}-{random}",
  "planId": "P000001",
  "stage": "S1",
  "skillId": "S01",
  "skillName": "需求边界界定",
  "executionMode": "normal",
  "dependencies": {
    "previousStageOutput": { "artifactId": "...", "artifactPath": "..." },
    "upstreamSkills": [{ "skillId": "S01", "artifactId": "..." }],
    "userInputs": []
  },
  "inputData": { "原始需求": "...", "上下文": {} },
  "outputConfig": {
    "templatePath": "skills/s1-requirements/skill1-boundary/template.md",
    "outputDir": "database/stages/s1/",
    "artifactId": "P000001-S1-S01-001"
  },
  "retryInfo": { "isRetry": false, "retryCount": 0 }
}
```

**执行结果格式**：
```json
{
  "resultId": "RES-{timestamp}-{random}",
  "executionId": "EXEC-xxx",
  "planId": "P000001",
  "stage": "S1",
  "skillId": "S01",
  "status": "success|failed",
  "artifact": {
    "artifactId": "P000001-S1-S01-001",
    "filePath": "database/stages/s1/P000001-S1-S01-001.md",
    "fileSize": 2048
  },
  "executionInfo": {
    "startTime": "2026-03-24T10:00:00Z",
    "endTime": "2026-03-24T10:00:30Z",
    "duration": 30
  }
}
```

---

## 三、主协调Agent调度规则

### 3.1 主执行流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              主执行流程                                      │
└─────────────────────────────────────────────────────────────────────────────┘

[开始]
   │
   ▼
┌─────────────────────────────────────┐
│ 阶段1：初始化阶段                     │
│ 1.1 接收用户原始需求                   │
│ 1.2 执行Skill0制定Plan                │
│ 1.3 初始化状态文件(status/resume)      │
│ 1.4 执行信息完整度评分                 │
│ 1.5 确定执行模式(normal/lightweight)   │
└─────────────┬─────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ 阶段2：阶段执行循环                   │
│                                     │
│  对于每个阶段 SX (S1→S2→S3→S4):      │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ 2.1 读取上一阶段汇总产物ID    │    │
│  │ 2.2 按顺序执行阶段内Skill:    │    │
│  │                              │    │
│  │   Skill执行循环:              │    │
│  │   a. 前置校验 → Validator    │    │
│  │   b. Skill执行 → Executor    │    │
│  │   c. 后置校验 → Validator    │    │
│  │   d. 保存产物，更新status     │    │
│  │   e. 记录执行日志             │    │
│  │                              │    │
│  │ 2.3 阶段完成后更新Plan状态    │    │
│  │ 2.4 生成阶段汇总产物          │    │
│  └─────────────────────────────┘    │
└─────────────┬─────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│ 阶段3：结果整合阶段                   │
│ 3.1 收集所有阶段产物                   │
│ 3.2 整合核心需求提炼表                 │
│ 3.3 生成最终报告                      │
│ 3.4 更新状态为completed               │
│ 3.5 输出结果                          │
└─────────────────────────────────────┘
   │
   ▼
[结束]
```

### 3.2 信息完整度评分规则

**评分维度**（满分100分，≥90分触发轻量化模式）：

| 维度 | 分值 | 评分标准 | 检查要点 |
|------|------|----------|----------|
| **边界清晰度** | 25分 | 产品目标/目标受众/核心场景，每项8分 | 是否明确产品做什么、为谁做、在什么场景使用 |
| **需求具体度** | 25分 | 显性功能/非功能需求，每项12分 | 是否列出具体功能点和性能/安全/体验要求 |
| **约束明确度** | 25分 | 时间/预算/技术/资源约束，每项6分 | 是否明确交付时间、预算范围、技术栈、团队规模 |
| **目标明确度** | 25分 | 核心价值/商业目标/落地指标，每项12分 | 是否说明解决什么问题、带来什么价值、如何衡量成功 |

**评分流程**：
```
1. 解析用户原始需求文本
2. 按四个维度逐一评分
3. 计算总分（各维度得分之和）
4. 判定执行模式：
   - 总分 ≥ 90分 → lightweight模式
   - 总分 < 90分 → normal模式
5. 将评分结果写入status.json的metadata.informationScore
```

### 3.3 双模式切换逻辑

| 模式 | 标识 | 说明 | 适用场景 |
|------|------|------|----------|
| **常规模式** | `normal` | 完整执行所有13个Skill，进行全面需求分析 | 需求信息不完整，需要深入分析 |
| **轻量化模式** | `lightweight` | 跳过部分Skill，快速输出核心需求 | 需求信息完整（≥90分），追求效率 |

**轻量化模式跳过的Skill**：

| 阶段 | 跳过的Skill | 原因 |
|------|------------|------|
| S1 | Skill3-隐性需求挖掘 | 信息已足够完整，无需额外挖掘 |
| S2 | Skill9-竞品分析 | 聚焦核心需求，减少市场分析 |
| S2 | Skill10-市场痛点验证 | 信息已明确，跳过验证 |

**执行顺序对比**：
- **常规模式**：S0 → S1(S01→S02→S03→S04) → S2(S09→S10) → S3(S07→S11→S12) → S4(S05→S06→S08)
- **轻量化模式**：S0 → S1(S01→S02→S04) → S3(S07→S11→S12) → S4(S05→S06→S08)

### 3.4 阶段化执行规则

#### S1阶段：需求边界与原始采集

```
Skill1(需求边界界定)
    ├── 输入：Plan定义（来自Skill0）
    ├── 输出：边界清单 → 产物ID: {PlanID}-S1-S01-001
    └── 下游：Skill2
         │
         ▼
Skill2(显性需求提取)
    ├── 输入：边界清单（Skill1产物）
    ├── 输出：显性需求表 → 产物ID: {PlanID}-S1-S02-001
    └── 下游：Skill3（常规模式）/ Skill4（轻量化模式）
         │
         ▼
Skill3(隐性需求挖掘) [轻量化模式跳过]
    ├── 输入：显性需求表（Skill2产物）
    ├── 输出：隐性需求表 → 产物ID: {PlanID}-S1-S03-001
    └── 下游：Skill4
         │
         ▼
Skill4(需求验证)
    ├── 输入：显性需求表 + 隐性需求表（常规）/ 显性需求表（轻量化）
    ├── 输出：最终需求原始表 → S1汇总产物 → 传递给S2
    └── 触发：节点2用户确认
```

#### S2阶段：市场与需求价值校验

```
Skill9(竞品分析) [轻量化模式跳过]
    ├── 输入：最终需求原始表（S1汇总产物）
    ├── 输出：竞品分析表 → 产物ID: {PlanID}-S2-S09-001
    └── 下游：Skill10
         │
         ▼
Skill10(市场痛点验证) [轻量化模式跳过]
    ├── 输入：竞品分析表（Skill9产物）
    ├── 输出：市场价值校验清单 → S2汇总产物 → 传递给S3
    └── 触发：节点2用户确认
```

**轻量化模式S2阶段处理**：
- 跳过Skill9和Skill10
- 将S1汇总产物直接传递给S3阶段
- 在status.json中标记S2阶段为"skipped"

#### S3阶段：技术可行性与选型

```
Skill7(需求风险识别)
    ├── 输入：S2汇总产物（或S1汇总产物-轻量化模式）
    ├── 输出：风险清单 → 产物ID: {PlanID}-S3-S07-001
    └── 下游：Skill12
         │
Skill11(技术可行性评估)
    ├── 输入：S2汇总产物（或S1汇总产物-轻量化模式）
    ├── 输出：可行性评估表 → 产物ID: {PlanID}-S3-S11-001
    └── 下游：Skill12
         │
         ▼
Skill12(轻量化技术选型)
    ├── 输入：风险清单 + 可行性评估表
    ├── 输出：技术选型及成本表 → S3汇总产物 → 传递给S4
    └── 触发：节点3风险决策（如存在高风险）
```

#### S4阶段：需求整合与核心提炼

```
Skill5(需求分类梳理)
    ├── 输入：技术选型及成本表（S3汇总产物）
    ├── 输出：分类表 → 产物ID: {PlanID}-S4-S05-001
    └── 下游：Skill6
         │
         ▼
Skill6(需求优先级排序)
    ├── 输入：分类表（Skill5产物）
    ├── 输出：优先级表 → 产物ID: {PlanID}-S4-S06-001
    └── 下游：Skill8
         │
         ▼
Skill8(核心需求提炼)
    ├── 输入：优先级表（Skill6产物）
    ├── 输出：核心需求提炼表 → 最终产物
    └── 触发：节点2用户确认
```

---

## 四、Skill间数据流转规则

### 4.1 数据流转总图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              数据流转总图                                     │
└─────────────────────────────────────────────────────────────────────────────┘

Skill0 (Plan制定)
    └── 输出：Plan定义 → 保存到 database/plans/{PlanID}/
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ S1阶段：需求边界与原始采集                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
Skill1 (边界界定) 
    └── 输出：边界清单 → {PlanID}-S1-S01-001
                              │
                              ▼
Skill2 (显性需求)
    ├── 输入：读取Skill1产物
    └── 输出：显性需求表 → {PlanID}-S1-S02-001
                              │
                              ├──(常规模式)──┐
                              ▼              │
                         Skill3 (隐性需求)   │
                             ├── 输入：读取Skill2产物
                             └── 输出：隐性需求表 → {PlanID}-S1-S03-001
                                                 │
                              ┌──────────────────┘
                              ▼
Skill4 (需求验证)
    ├── 输入：读取Skill2+Skill3产物（常规）/ 读取Skill2产物（轻量化）
    ├── 输出：最终需求原始表 → S1汇总产物
    └── 传递 ────────────────────────────────────────┐
                                                     │
                              ┌──────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ S2阶段：市场与需求价值校验                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
            ┌─────────────────┴─────────────────┐
            │(常规模式)                         │(轻量化模式)
            ▼                                   ▼
    Skill9 (竞品分析)                    (跳过S2阶段)
        ├── 输入：读取S1汇总产物           直接传递S1汇总产物到S3
        └── 输出：竞品分析表 → {PlanID}-S2-S09-001
                              │
                              ▼
    Skill10 (市场验证)
        ├── 输入：读取Skill9产物
        ├── 输出：市场价值校验清单 → S2汇总产物
        └── 传递 ────────────────────────────────────────┐
                                                         │
                              ┌──────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ S3阶段：技术可行性与选型                                                      │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
    Skill7 (风险识别) ←───────┘
        ├── 输入：读取S2汇总产物（或S1汇总产物-轻量化）
        └── 输出：风险清单 → {PlanID}-S3-S07-001 ────────┐
                                                         │
    Skill11 (可行性评估) ←───────────────────────────────┘
        ├── 输入：读取S2汇总产物（或S1汇总产物-轻量化）
        └── 输出：可行性评估表 → {PlanID}-S3-S11-001 ────┤
                                                         │
                              ┌──────────────────────────┘
                              ▼
    Skill12 (技术选型)
        ├── 输入：读取Skill7+Skill11产物
        ├── 输出：技术选型及成本表 → S3汇总产物
        └── 传递 ────────────────────────────────────────┐
                                                         │
                              ┌──────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ S4阶段：需求整合与核心提炼                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
    Skill5 (分类梳理)
        ├── 输入：读取S3汇总产物
        └── 输出：分类表 → {PlanID}-S4-S05-001
                              │
                              ▼
    Skill6 (优先级排序)
        ├── 输入：读取Skill5产物
        └── 输出：优先级表 → {PlanID}-S4-S06-001
                              │
                              ▼
    Skill8 (核心提炼)
        ├── 输入：读取Skill6产物
        └── 输出：核心需求提炼表 → 最终产物
```

### 4.2 产物ID生成规则

#### Plan ID生成
```
格式：P{6位数字}
示例：P000001

生成方式：
  1. 取当前时间戳后6位
  2. 加上2位随机数
  3. 取模确保6位数字
```

#### Skill产物ID生成
```
格式：{PlanID}-{阶段编号}-{Skill编号}-{序号}
示例：P000001-S1-S01-001

组成部分：
  - PlanID: 所属Plan的ID (P[0-9]{6})
  - 阶段编号: S0/S1/S2/S3/S4
  - Skill编号: S00-S12
  - 序号: 3位数字，从001开始递增
```

### 4.3 产物文件存储规则

| 产物类型 | 存储路径 | 文件名格式 |
|---------|----------|-----------|
| Plan定义 | `database/plans/{PlanID}/` | `plan.md` |
| S1阶段产物 | `database/stages/s1/` | `{PlanID}-S1-S0X-00X.md` |
| S2阶段产物 | `database/stages/s2/` | `{PlanID}-S2-S0X-00X.md` |
| S3阶段产物 | `database/stages/s3/` | `{PlanID}-S3-S0X-00X.md` |
| S4阶段产物 | `database/stages/s4/` | `{PlanID}-S4-S0X-00X.md` |
| 阶段汇总产物 | `database/stages/{sX}/` | `{PlanID}-S{X}-summary.md` |

---

## 五、断点续跑流程

### 5.1 断点检测流程

```
每次执行前：
  1. 检查 database/plans/{PlanID}/resume.json 是否存在
  2. 若不存在 → 正常从头执行
  3. 若存在：
     a. 读取 resume.json
     b. 检查 canResume 字段
     c. 若 canResume=false → 正常从头执行
     d. 若 canResume=true → 进入断点恢复流程
```

### 5.2 断点恢复流程

```
读取resume.json获取断点信息
    │
    ▼
检查断点有效性：
  ├─ 检查lastCompletedOutput是否存在
  ├─ 检查当前Skill的输入依赖是否满足
  └─ 检查重试次数是否超过限制(maxRetries=3)
    │
    ├── 检查通过 ──→ 恢复执行上下文(context.dataSnapshot)
    │                  从断点位置继续执行
    │                  更新resume.json: resumedAt=当前时间
    │
    └── 检查失败 ──→ 根据recoveryStrategy执行恢复策略
                       或提示用户决策
```

### 5.3 断点保存时机

在以下情况下保存断点：

| 情况 | 触发条件 | resumeStatus |
|------|----------|--------------|
| 用户主动暂停 | 用户输入"暂停"或"保存进度" | `user_paused` |
| Skill执行失败 | Executor Agent返回错误 | `failed` |
| 校验失败 | Validator Agent返回校验不通过 | `validation_failed` |
| 等待用户交互 | UserInteraction Agent等待用户输入超时 | `waiting_user` |
| 系统异常 | 发生意外错误 | `error` |

### 5.4 断点保存操作

```
1. 更新 resume.json:
   {
     "canResume": true,
     "resumeStatus": "interrupted|failed|user_paused",
     "breakpoint": {
       "stage": "当前阶段",
       "skill": "当前Skill",
       "action": "中断时的动作",
       "interruptedAt": "当前时间",
       "reason": "中断原因",
       "retryCount": "重试次数+1"
     },
     "context": {
       "lastCompletedStage": "最后完成的阶段",
       "lastCompletedSkill": "最后完成的Skill",
       "lastCompletedOutput": "最后产出的产物ID",
       "dataSnapshot": "当前执行上下文快照"
     }
   }

2. 更新 status.json:
   {
     "status": "paused",
     "updatedAt": "当前时间"
   }
```

### 5.5 恢复策略

| 中断类型 | 恢复策略 | 说明 |
|----------|----------|------|
| 用户主动暂停 | 从断点继续 | 完全恢复中断时的状态 |
| Skill执行错误 | 重试当前Skill | retryCount < 3时重试，否则跳过或终止 |
| 校验失败 | 重试或用户决策 | 根据错误类型选择重试或询问用户 |
| 用户交互超时 | 等待用户输入 | 保持断点，等待用户响应 |
| 依赖产物缺失 | 重新执行依赖Skill | 回溯到缺失产物的Skill重新执行 |

---

## 六、异常处理流程

### 6.1 错误分类

| 错误类型 | 说明 | 处理策略 | 严重级别 |
|----------|------|----------|----------|
| **输入错误** | 输入数据格式不正确 | 返回错误，要求重新输入 | 中 |
| **执行错误** | Skill执行失败 | 重试3次，仍失败则保存断点 | 高 |
| **校验错误** | 产物格式不符合规范 | 尝试自动修复，或返回修改 | 中 |
| **依赖错误** | 前置产物不存在 | 回溯执行前置Skill | 高 |
| **系统错误** | 文件读写等系统问题 | 保存断点，提示用户 | 高 |

### 6.2 错误处理流程

```
捕获错误
    │
    ▼
分类错误类型
    │
    ▼
根据类型选择策略：
    ├─ 可恢复错误 ──→ 自动恢复/重试
    │
    ├─ 需用户决策 ──→ 调用UserInteraction Agent
    │
    └─ 严重错误 ────→ 保存断点，终止执行
    │
    ▼
记录错误日志
    │
    ▼
更新状态文件
```

### 6.3 重试机制

**重试规则**：
- 最大重试次数：3次
- 重试间隔：指数退避（1s → 2s → 4s）
- 重试条件：执行错误、校验错误（可自动修复类）

**重试流程**：
```
执行失败
    │
    ▼
retryCount < 3?
    ├── 是 ──→ 等待退避时间
    │          清理上一次产物
    │          重新执行Skill
    │          retryCount += 1
    │
    └── 否 ──→ 保存断点
               提示用户决策
```

---

## 七、状态文件管理

### 7.1 状态文件清单

| 文件 | 路径 | 用途 | 更新时机 |
|------|------|------|----------|
| **status.json** | `database/plans/{PlanID}/status.json` | 需求分析流程状态 | Skill执行后、阶段完成后 |
| **resume.json** | `database/plans/{PlanID}/resume.json` | 断点续跑配置 | 断点保存时、恢复执行时 |
| **index.json** | `database/index.json` | 全局信息库索引 | Plan创建时、产物生成时 |
| **execution.log** | `database/plans/{PlanID}/execution.log` | 执行日志 | 每次操作后 |

### 7.2 status.json 结构

```json
{
  "planId": "P000001",
  "status": "running|paused|completed|failed",
  "currentStage": "S1|S2|S3|S4",
  "currentSkill": "S01|S02|...",
  "executionMode": "normal|lightweight",
  "metadata": {
    "informationScore": 85,
    "createdAt": "2026-03-24T10:00:00Z",
    "updatedAt": "2026-03-24T10:30:00Z"
  },
  "stages": {
    "s1": {
      "status": "completed|running|pending|skipped",
      "skills": {
        "s01": { "status": "completed", "outputId": "P000001-S1-S01-001" },
        "s02": { "status": "completed", "outputId": "P000001-S1-S02-001" },
        "s03": { "status": "completed|skipped", "outputId": "..." },
        "s04": { "status": "running", "outputId": null }
      }
    },
    "s2": { "...": "..." },
    "s3": { "...": "..." },
    "s4": { "...": "..." }
  },
  "progress": {
    "totalStages": 4,
    "completedStages": 1,
    "totalSkills": 13,
    "completedSkills": 3
  }
}
```

### 7.3 resume.json 结构

```json
{
  "planId": "P000001",
  "canResume": true,
  "resumeStatus": "interrupted|failed|user_paused",
  "breakpoint": {
    "stage": "S1",
    "skill": "S04",
    "action": "execute|validate|retry",
    "interruptedAt": "2026-03-24T10:30:00Z",
    "reason": "用户主动暂停",
    "retryCount": 0
  },
  "context": {
    "lastCompletedStage": "S1",
    "lastCompletedSkill": "S03",
    "lastCompletedOutput": "P000001-S1-S03-001",
    "dataSnapshot": { "执行上下文快照": "..." }
  },
  "recoveryStrategy": "resume_from_breakpoint",
  "resumedAt": null
}
```

---

## 八、用户交互节点

### 8.1 交互节点总览

| 节点 | 触发阶段 | 触发Skill | 触发条件 | 交互类型 |
|------|---------|----------|----------|----------|
| **节点1** | S1 | Skill1/Skill2 | 关键字段缺失/模糊 | 定向补全提问 |
| **节点2** | S1/S2/S4 | Skill4/Skill10/Skill8 | 核心产物需确认 | 表格确认 |
| **节点3** | S3 | Skill7/Skill11 | 高风险项需决策 | 风险决策 |

### 8.2 节点1：S1阶段信息补全

**触发场景**：
- Skill1执行后，核心字段（产品目标/目标受众/核心场景/资源约束）为空或模糊
- Skill2执行后，显性需求内容不完整或需求来源不明确

**交互形式**：定向补全提问

**提问模板**：
```
【需求分析-{阶段}-{Skill}】

当前执行阶段：{阶段名称}
当前执行步骤：{Skill名称}

检测到「{字段名}」信息缺失/模糊，需要补充。

请按以下格式补充：
{格式要求}

当前上下文：
{相关上下文信息}
```

### 8.3 节点2：核心产物确认

**触发场景**：
- Skill4执行完成后，需用户确认《最终需求原始表》
- Skill10执行完成后，需用户确认市场价值校验结果
- Skill8执行完成后，需用户确认核心需求提炼结果

**交互形式**：标准化表格+确认提问

**提问模板**：
```
【需求分析-{阶段}-{Skill}】

{产物名称}已生成，请确认：

{产物表格内容}

请确认以上结果：
- 输入「确认」表示无误，继续执行
- 输入「需修改」并按要求格式提供修改内容

修改格式：「需求序号+修正内容」，多个用分号分隔
示例：「1+支持英语刷题；2+取消成绩排行榜」
```

### 8.4 节点3：S3阶段风险决策

**触发场景**：
- Skill7标记高风险项
- Skill11判定为「需论证」或「不可行」
- 技术实现成本超出预期预算

**交互形式**：定向决策提问

**提问模板**：
```
【需求分析-{阶段}-{Skill}】

检测到高风险项，需要您决策：

风险详情：
- 涉及需求：{需求内容}
- 风险类型：{风险类型}
- 风险等级：{高/中/低}
- 风险描述：{详细描述}
- 影响评估：{影响说明}

请选择处理方式：
- 输入「保留」：保留该需求，按原计划执行
- 输入「剔除」：剔除该需求，后续阶段不再考虑
- 输入「降级」：降级为简易版，降低实现复杂度

若选择「降级」，请补充降级后的简易版描述。
```

---

## 九、执行日志规范

### 9.1 日志级别

| 级别 | 使用场景 | 示例 |
|------|----------|------|
| **INFO** | 正常流程节点（开始/完成） | `[INFO] Skill1执行完成` |
| **WARN** | 警告信息（如重试、跳过） | `[WARN] S2阶段在轻量化模式下被跳过` |
| **ERROR** | 错误信息（执行失败） | `[ERROR] Skill3执行失败：输入数据格式错误` |
| **DEBUG** | 调试信息（详细执行过程） | `[DEBUG] 加载输入数据：{...}` |

### 9.2 日志格式

```
[时间戳] [级别] [阶段-Skill] 消息内容

示例：
[2026-03-24 10:00:00] [INFO] [S1-S01] 开始执行Skill1：需求边界界定
[2026-03-24 10:00:30] [INFO] [S1-S01] Skill1执行完成，产物ID: P000001-S1-S01-001
[2026-03-24 10:01:00] [WARN] [S1-S02] 输入数据缺少部分字段，使用默认值
[2026-03-24 10:02:00] [ERROR] [S1-S03] 执行失败：依赖产物不存在
[2026-03-24 10:02:01] [INFO] [SYSTEM] 断点已保存，可从S1-S03继续
```

### 9.3 日志存储

- **路径**：`database/plans/{PlanID}/execution.log`
- **格式**：纯文本，每行一条日志
- **轮转**：单文件超过10MB时创建新文件（execution-1.log, execution-2.log...）

---

## 十、初始化检查清单

在开始执行需求分析流程前，检查以下内容：

### 10.1 环境检查

- [ ] `database/index.json` 存在且可读取
- [ ] `database/plans/` 目录存在且可写入
- [ ] `database/stages/` 目录存在且可写入
- [ ] `agents/coordinator.md` 已加载
- [ ] `agents/validator.md` 已加载
- [ ] `agents/user-interaction.md` 已加载
- [ ] `agents/executor.md` 已加载

### 10.2 Skill定义检查

- [ ] 所有Skill定义文件路径正确
- [ ] `skills/skill0-plan/SKILL.md` 存在
- [ ] `skills/s1-requirements/skill1-boundary/SKILL.md` 存在
- [ ] `skills/s1-requirements/skill2-explicit/SKILL.md` 存在
- [ ] `skills/s1-requirements/skill3-implicit/SKILL.md` 存在
- [ ] `skills/s1-requirements/skill4-validation/SKILL.md` 存在
- [ ] `skills/s2-market/skill9-competitor/SKILL.md` 存在
- [ ] `skills/s2-market/skill10-market/SKILL.md` 存在
- [ ] `skills/s3-technical/skill7-risk/SKILL.md` 存在
- [ ] `skills/s3-technical/skill11-feasibility/SKILL.md` 存在
- [ ] `skills/s3-technical/skill12-selection/SKILL.md` 存在
- [ ] `skills/s4-integration/skill5-classify/SKILL.md` 存在
- [ ] `skills/s4-integration/skill6-priority/SKILL.md` 存在
- [ ] `skills/s4-integration/skill8-core/SKILL.md` 存在

### 10.3 工具检查

- [ ] 产物ID生成器可用
- [ ] 日志记录功能正常
- [ ] 文件读写权限正常

---

## 十一、最终输出

### 11.1 输出产物清单

| 产物 | 类型 | 说明 |
|------|------|------|
| **核心需求提炼表** | 必需 | Skill8产物，包含最终的核心需求列表 |
| **完整需求分析报告** | 必需 | 整合所有阶段产物的完整报告 |
| **执行总结** | 必需 | 包含执行时间、模式、各阶段完成情况的总结 |

### 11.2 状态文件最终状态

执行完成后，确保以下文件已正确更新：

```json
// status.json
{
  "planId": "P000001",
  "status": "completed",
  "currentStage": "S4",
  "currentSkill": "S08",
  "progress": {
    "completedStages": 4,
    "completedSkills": 13
  }
}

// resume.json
{
  "planId": "P000001",
  "canResume": false,
  "resumeStatus": "completed"
}
```

---

## 十二、附录

### 12.1 Skill清单

| 编号 | 名称 | 阶段 | 轻量化模式 |
|------|------|------|-----------|
| S00 | Plan制定 | S0 | 执行 |
| S01 | 需求边界界定 | S1 | 执行 |
| S02 | 显性需求提取 | S1 | 执行 |
| S03 | 隐性需求挖掘 | S1 | **跳过** |
| S04 | 需求验证 | S1 | 执行 |
| S09 | 竞品分析 | S2 | **跳过** |
| S10 | 市场痛点验证 | S2 | **跳过** |
| S07 | 需求风险识别 | S3 | 执行 |
| S11 | 技术可行性评估 | S3 | 执行 |
| S12 | 轻量化技术选型 | S3 | 执行 |
| S05 | 需求分类梳理 | S4 | 执行 |
| S06 | 需求优先级排序 | S4 | 执行 |
| S08 | 核心需求提炼 | S4 | 执行 |

### 12.2 相关文档索引

| 文档 | 路径 | 说明 |
|------|------|------|
| Plan.md | `./Plan.md` | 项目实施计划 |
| 主协调Agent | `./agents/coordinator.md` | Agent调度规则详情 |
| 校验守卫Agent | `./agents/validator.md` | 校验规则详情 |
| 用户交互Agent | `./agents/user-interaction.md` | 交互规则详情 |
| 技能执行Agent | `./agents/executor.md` | 执行规则详情 |
| 信息库索引 | `./database/index.json` | 全局索引 |

---

*文档版本：1.0*
*最后更新：2026-03-24*
*编写者：Agent+Skill系统*
